FECHA; GERENCIA CAM; SUB GERENCIA CAM; AREA DE CAM; EMPRESA CONTRATISTA; Nº ENC; Q NC; N IPAL; RESPUESTAS SI; RESPUESTAS NO; RESPUESTAS N/A; INSPECTOR;PREVENCIONISTA;CIERRE;CONTRATO;SUPERVISOR;LUGAR;CHARLA_OPERATIVA;TIPO;AUTO_INSPECCION;CIERRE_TEXTO;ACTIVIDAD
{% for item in data %}
"{{ item.fecha | date("m-Y") }}";"{{ item.gerencia | escape }}";"{{ item.subgerencia | escape }}";"{{ item.area | escape }}";"{{ item.contratista | escape }}";1;{{ item.cantidad_incumplimientos }};{{ item.ipal }};{{ item.respuestas_0 }};{{ item.respuestas_1 }};{{ item.respuestas_2 }};"{{-item.inspector| escape -}}";"{{-item.prevencionista | escape -}}";"{{-item.cierre | escape -}}";"{{-item.contrato | escape -}}";"{{-item.supervisor | escape -}}";"{{-item.lugar | escape -}}";"{{- item.charla_operativa | escape -}}";"{{-item.tipo | escape -}}";"{{-item.auto_inspeccion | escape -}}";"{{-item.cierre_texto | escape -}}";"{{-item.actividad | escape -}}"
{% endfor %}
