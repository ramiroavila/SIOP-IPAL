"ITEM",{% for key,label in valor %}"{{ label }}",{% endfor %}"{{ 'TIPO' }}"
{% for key, item in data %}"{{ key | trans }}"{% for valor_total, total_total in item %},"{{total_total}}"{% endfor %},"{{ tipo }}"{{'\n'}}{% endfor %}
