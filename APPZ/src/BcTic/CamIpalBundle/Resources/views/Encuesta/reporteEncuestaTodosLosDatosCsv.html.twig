NUMERO;FECHA; GERENCIA CAM; SUB GERENCIA CAM; AREA DE CAM; EMPRESA CONTRATISTA; Nº ENC; Q NC; N IPAL; RESPUESTAS SI; RESPUESTAS NO; RESPUESTAS N/A; INSPECTOR;PREVENCIONISTA;CIERRE;CONTRATO;SUPERVISOR;LUGAR;CHARLA_OPERATIVA;TIPO;AUTO_INSPECCION;CIERRE_TEXTO;ACTIVIDAD;OBSERVACIONES;TIPO_DE_HALLAZGO;EMPLEADOS;INCUMPLIMIENTOS
{% for item in data %}
"{{-item.id-}}";"{{ item.fecha | date("m-Y") }}";"{{ item.gerencia | escape }}";"{{ item.subgerencia | escape }}";"{{ item.area | escape }}";"{{ item.contratista | escape }}";1;{{ item.cantidad_incumplimientos }};{{ item.ipal }};{{ item.respuestas_0 }};{{ item.respuestas_1 }};{{ item.respuestas_2 }};"{{-item.inspector| escape -}}";"{{-item.prevencionista | escape -}}";"{{-item.cierre | escape -}}";"{{-item.contrato | escape -}}";"{{-item.supervisor | escape -}}";"{{-item.lugar | escape -}}";"{{- item.charla_operativa | escape -}}";"{{-item.tipo | escape -}}";"{{-item.auto_inspeccion | escape -}}";"{{-item.cierre_texto | escape -}}";"{{-item.actividad | escape -}}";"{{-item.observaciones-}}";"{{-item.tipo_de_hallazgo|trans-}}";"{{-item.empleados-}}";"{% for incumplimiento in item.incumplimientos %}{{-incumplimiento | trans -}}\n{% endfor %}"
{% endfor %}
